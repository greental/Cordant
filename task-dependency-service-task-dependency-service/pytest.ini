[pytest]
asyncio_mode = auto
markers =
    unit: pure unit tests, no Docker services
    integration: tests requiring real Docker MongoDB, Redis, or Kafka
    stress: long-running tests requiring the running stack; never run by default